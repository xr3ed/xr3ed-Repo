// use an integer for version numbers
version = 1

cloudstream {
    description = "MelongMovie provider"
    authors = listOf("sad25kag")

    /**
     * Status int as the following:
     * 0: Down
     * 1: Ok
     * 2: Slow
     * 3: Beta only
     * */
    status = 0

    tvTypes = listOf("Movie")

    iconUrl = "http://139.59.189.160/wp-content/uploads/2026/04/MelongMovie1.png"

    language = "id"
}
